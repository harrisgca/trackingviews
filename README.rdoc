== Useage

* Clone the repo ( git clone https://github.com/stefioan/trackingviews.git )

* Input "bundle install" in terminal at the project's directory

* Input "rake db:migrate"

* Run the "rails server"

* Open your browser at http://localhost:3000

Thank you for checking out this project!
